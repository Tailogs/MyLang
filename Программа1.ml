var a = 20.0
var b = 10
var c
var d

puts "Вычисляем выражение: (a + b) / c"
puts "Переменная a равна: "
puts a
puts "Переменная b равна: "
puts b
puts "Введите значение переменной c"
gets c

-- Вычисляем выражение, складывая его операнды 
-- и сохраняем результат в переменную d
a + b -> d

-- Вычисляем выражение, осуществляя деление его
-- операндов и повторно сохраняем результат в 
-- переменную d
d / c -> d

puts "Результат: "
puts d